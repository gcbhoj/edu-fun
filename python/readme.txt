pip install -r requirements.txt - This will install all the requirments for this application to execute.
flask --app server run - This will initialize the flask server
python -m Services.AgeUpdateServices -- runs services python file